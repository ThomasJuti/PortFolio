// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
  // Set `site` to your deployed URL once you have one — it powers the
  // canonical link and Open Graph tags generated in BaseLayout.astro.
  // site: 'https://your-domain.com',
});
