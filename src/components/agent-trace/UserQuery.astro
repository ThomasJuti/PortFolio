---
import { content } from '../../i18n/content';

const meta = content.en.audienceMeta[0];
const query = content.en.audiences[0].query;
---

<div class="query">
  <p class="from" id="query-from">{meta.from}</p>
  <p class="bubble">
    <span id="query-text">{query}</span><span class="cursor" aria-hidden="true">▌</span>
  </p>
</div>

<style>
  .query {
    animation: rise 0.6s ease both;
    margin-bottom: 40px;
  }

  .from {
    font-size: 12px;
    color: var(--color-text-faint);
    margin: 0 0 10px;
  }

  .bubble {
    background: var(--color-surface-alt);
    border: 1px solid var(--color-border-strong);
    border-radius: 12px 12px 12px 2px;
    padding: 20px 26px;
    font: 500 17px var(--font-sans);
    color: var(--color-text);
    display: inline-block;
    margin: 0;
  }

  .cursor {
    color: var(--color-accent);
    animation: blink 1.1s step-end infinite;
  }
</style>
